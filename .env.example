# Copy this file to .env and fill in your key
# The Anthropic API key is called from the browser (direct access).
# Only use this in a local / demo context — never expose keys in production.
VITE_ANTHROPIC_API_KEY=your_anthropic_api_key_here
